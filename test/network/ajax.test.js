/*global describe, it, expect*/
describe('ajax', function () {
    beforeEach();
    it('simple get', function () {
        var ajax = require('../../lib/network/ajax.js'),
            sinon = require('../../node_modules/sinon/lib/sinon.js');
        expect(true).toEqual(true);
    });
});
